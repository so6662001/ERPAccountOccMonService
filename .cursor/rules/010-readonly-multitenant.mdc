---
description: 只读安全与多租户访问铁律（涉及数据源/查询/引擎时务必遵守）
alwaysApply: true
---

# 只读安全与多租户铁律

## 业务库访问（不可违背）
- 一切业务库访问只能是 `SELECT`/CTE；在执行前用 SQL 解析强制校验，拦截 INSERT/UPDATE/DELETE/DDL/调用有副作用的存储过程。
- 通过 `ReadOnlyDataSourceRouter` 路由；优先连只读副本/备库；连接池(HikariCP)按客户隔离并限流；设置查询超时与最大返回行数。
- 业务库口令加密存储(KMS/AES)，运行时解密；日志、接口、异常信息中**绝不回显口令**。
- 平台库（写）与业务库（只读）严格隔离，禁止把平台数据写进业务库，反之亦然。

## 多租户
- 独立库模式：按"客户"运行时路由到独立 DataSource。
- 租户隔离模式：每条查询自动注入 `tenant_id = :tenantId` 过滤；任何遗漏租户过滤的查询都视为缺陷；严禁跨租户串数据。
- 高效扫描：同一规则对租户群优先用 `GROUP BY tenant_id HAVING <违规条件>` 一次定位异常租户，再仅对异常租户下钻明细。禁止"循环逐租户各查一次"的实现。

## 金额与容差
- 金额一律 `BigDecimal`(DECIMAL(20,4))；相等判定用 `abs(a-b) <= tolerance`(默认 0.005)；NULL 统一视为 0（口径全局一致）。

## 参数化
- 所有动态条件(:period/:tenantId/科目等)使用参数绑定，杜绝 SQL 拼接注入。
